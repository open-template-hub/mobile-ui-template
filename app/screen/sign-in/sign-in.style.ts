import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  orBeClassical: {
    marginTop: 15,
    marginBottom: 15,
  },
  orBeClassicalLabel: {
    fontWeight: '700',
    textAlign: 'center',
    fontSize: 14,
    color: '#616161',
  },
  mainContainer: {
    marginTop: '30%',
  },
});
